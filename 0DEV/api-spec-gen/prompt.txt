You are an API designer. Generate a complete **OpenAPI 3.0.3** specification in **YAML only** (no commentary) for **Energy Trading API** with:
- Entities: trader, Trade-price, Trade-date, BusinessUnit, Error
- CRUD endpoints for each, plus approve/cancel for trading
- Pagination & filters as described
- CamelCase JSON
- Request/response bodies with examples for 200 responses
- Proper status codes (201 on create, 422 validation, etc.)
- components/schemas reused via $ref
- securitySchemes: apiKey in header `Ocp-Apim-Subscription-Key`
- servers: https://api.example.corp/mw/v1
- Ensure all $ref resolve and path params are valid
